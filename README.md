# Rede Neural

## Descrição

Um gerador rede neural simples desenvolvida em Java, no NetBeans. Ele carrega um CSV com um conjunto de treinamento, cria uma rede neural com os parâmetros que o usuário definiu e depois e treina a rede. Ele também pode carregar um CSV com um conjunto de testes e gerar uma matriz de confusão com o resultado.

